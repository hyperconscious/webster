description: Invalid or expired token or user does not exist.
content:
  application/json:
    schema:
      type: object
      properties:
        statusCode:
          type: integer
          example: 401
        method:
          type: string
          example: "POST"
        message:
          type: string
          example: "Unauthorized request"